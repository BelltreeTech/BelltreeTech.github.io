# Open Variables の検証

2026-09-08に、前の公開版「対話の余白」を基準にして追加探索・反論の実装・ブラウザーでの批評を行った結果です。訂正済みの公開データは変更していません。試作、比較画像、個別のログ、非公開の確認記録はGit・配信物の対象外です。

| 検証 | 結果 |
| --- | --- |
| lint / 内容 / build検査 | 成功。内容6件・build4件 |
| Chromium 153.0.8010.12 / WebKit 26.6 | 320〜1920pxの21幅をそれぞれ通過 |
| 最後の導入文の折返し調整後 | 両エンジンで320 / 390 / 768 / 960 / 1440pxを再確認、成功 |
| axe | 全幅検証・最終再確認とも各エンジン6スキャン、確定違反0 |
| 公開内容 | 研究1件・インターン3件・活動記録9件・資格1件・メディア3件を保持。元の公開文182項目に欠落なし |
| 依存監査 | 実行時点の報告された脆弱性0。依存パッケージの変更なし |

確認幅は320 / 360 / 390 / 430 / 639 / 640 / 641 / 768 / 959 / 960 / 961 / 1024 / 1199 / 1200 / 1201 / 1280 / 1440 / 1799 / 1800 / 1801 / 1920 CSS pxです。

6章、13個の詳細、実績5分類と件数、直接リンク、再読み込み、戻る・進む、非該当フィルターから履歴を使った記録への復帰を検査しました。研究・経験・考え方の焦点切替は、内容・選択状態・対応するリンク、キーボード操作を確認しています。PC/mobileで全本文を独立取得して一致を照合し、既存の架空・保留項目の不在検査を維持しています。

キーボード、Escape、スキップリンク、リサイズと縦横の変更、開閉状態、44px以上の主要操作、動きを減らす設定、200%文字サイズを確認しました。見出しと紹介枠の非重なり、フォント読み込み、フォント取得失敗時の本文と操作も検査しています。

axeの要確認は、PCの選択中Contactリンクの擬似要素（下線）の背景判定1件が両エンジンで残りました。実際の前景 `#626158` と背景 `#f3f0e7` は5.4683:1、文字と下線は約5.4px離れ、独立した色計算と実画面確認では通常文字の4.5:1を満たします。確定違反0は完全なアクセシビリティ認証を意味しません。

配信物はHTML・JavaScript・CSS・Inter Latin・favicon・Interライセンスの6ファイル、308,671 bytesです。最終検証版は `index-CcOvMjc6.js` / `index-DpIx6_i3.css`。原本PDF、私的記録、実験用ソース、比較画像、不要なフォント、前版の抽象画像は含みません。Gitへの公開候補は配信物と別に確認しています。

追加探索では、現行の発展案、観測室の再構成、実内容を持つ括弧の案を全章で比較しました。暫定案に対し、氏名を主役にした版と、担当・学びを併読できる版を実装して反論を検証。PCでは経験の3列組版、mobileでは初期の学び表示、考え方の本文の近さ、連絡先の着地を改善しました。3社の学びを両版とも開いて節冒頭へ揃えた画像でも比較し、初期openによる利点と組版による利点を区別しています。

同じローカルHTTP・Chromium・画面幅・キャッシュ条件で、両版それぞれPC/mobile各3組の初回・再訪、計24サンプルを測定しました。初回転送は358,270→305,187 bytes（約14.8%減）、通常ページのリクエストは6→4。JavaScriptは222,013→223,886 bytes、DOMは618→644要素で、焦点切替と読める内容の配置のための小さな増加があります。FCP/LCP中央値はPC初回64→52ms、mobile幅初回64→48ms。50ms超のlong taskは両版とも0でした。クリックから2フレームまでの値やCPU合計には増減があり、全操作が高速化したとは判断しません。これらは非圧縮のローカル環境での少数試験で、実機やCDN経由の体感値・INPではありません。

環境はmacOS arm64、Node.js24、npm11、Playwright1.63.0。iPhone・Android実機、Safariアプリそのもの、Windows・Linux、実スクリーンリーダー、ブラウザーUIによるズーム、OSメールアプリの起動・送信は未検証です。200%はルート文字サイズ32pxによる検査で、mobile幅は実機の性能を再現しません。メールとSNSは実クリックを傍受して行き先を確認し、送信していません。

本番公開では同じGitHub PagesのURLと方式を使い、描画・操作と配信ファイルのハッシュを別途照合します。ソース・配信の識別子と復旧手順はローカルのリリース記録へ保存します。復旧先には事実訂正済みの直前版のみを使います。
